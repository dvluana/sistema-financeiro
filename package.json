{
  "name": "sistema-financeiro",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run kill-ports && concurrently -n api,web -c blue,magenta --kill-others-on-fail --kill-others --handle-input \"npm run dev:api\" \"npm run dev:web\"",
    "dev:all": "npm run preflight && npm run dev",
    "dev:api": "npm run dev --workspace=apps/api",
    "dev:web": "npm run dev --workspace=apps/web",
    "preflight": "npm run health-check --workspace=apps/api",
    "kill-ports": "bash scripts/kill-ports.sh",
    "cleanup": "npm run kill-ports && (pkill -f 'tsx watch' || true) && (pkill -f 'vite' || true) && echo '✨ Processos limpos'",
    "build": "npm run build:api && npm run build:web",
    "build:api": "npm run build --workspace=apps/api",
    "build:web": "npm run build --workspace=apps/web",
    "start": "npm run start --workspace=apps/api",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "clean": "rm -rf apps/*/dist apps/*/node_modules/.vite",
    "heroku-postbuild": "npm run build:web && npm run build:api && cp -r apps/web/dist apps/api/dist/web"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.50.0",
    "@typescript-eslint/parser": "^8.50.0",
    "concurrently": "^8.2.2",
    "eslint": "^9.39.2",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-react": "^7.37.5",
    "eslint-plugin-react-hooks": "^7.0.1",
    "kill-port": "^2.0.1",
    "prettier": "^3.7.4",
    "sharp": "^0.34.5"
  }
}
